// Rendered while a route loader runs on initial hydration so React Router can
// keep the application shell painted without warning about a missing fallback.
export function HydrateFallback() {
  return <div className="pf-fit" />;
}
