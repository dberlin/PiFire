warm NLP solve cost, shipped config at t_step=25 s, 15 solves after a discarded cold start
the budget is control_period, NOT t_step: 5000 ms shipped, 1000 ms at controllers.json's minimum
the 'Pi 5' columns scale by an ASSUMED 6x slowdown to the nominal target

=== n_delay = 8 === state dimension 10
  n_horizon | horizon s | build s | avg ms | max ms | max % of 5 s | Pi 5 max % of 5 s
         24 |       600 |    2.14 |   36.8 |   38.4 |         0.77 |              4.61
         96 |      2400 |    0.39 |   88.0 |   90.8 |         1.82 |             10.89

=== n_delay = 12 === state dimension 14
  n_horizon | horizon s | build s | avg ms | max ms | max % of 5 s | Pi 5 max % of 5 s
         24 |       600 |    0.12 |   40.0 |   40.9 |         0.82 |              4.91
         96 |      2400 |    0.42 |  155.8 |  169.3 |         3.39 |             20.31

  worst solve at 96 steps, over both chain lengths: 169.3 ms measured,
  1016 ms at the assumed Pi 5 slowdown.
    vs control_period 5 s (shipped): 3.39 % measured, 20.3 % assumed
    vs control_period 1 s (minimum): 16.93 % measured, 101.6 % assumed  <-- OVER the period

  Overrunning control_period is a degraded cadence, not a loss of control: the worker
  loop calls update() again when the solve returns, so the controller re-solves less
  often than configured against a plant whose own time constants are minutes. The
  shipped cadence having margin is the load-bearing reading; the 1 s cadence is a
  documented cost of an operator setting, not a bound on what may be built.
