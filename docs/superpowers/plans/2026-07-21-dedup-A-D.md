# Duplication Cleanup A–D Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Eliminate the four highest-value, well-tested production code duplications found by ast_duplicate_detector (--min-lines=20), behavior-preserving.

**Tech Stack:** Python 3.14, pytest, uv, ruff. Baseline suite: 2453 passed.

## Global Constraints

- **Behavior-preserving.** No probe reading, no rendered pixel, no input dispatch changes. Verify against the existing suite (2453) + the relevant targeted tests.
- Test runner ALWAYS: `QT_QPA_PLATFORM=offscreen SDL_VIDEODRIVER=dummy uv run pytest <target>`.
- Registry is filename-based: `probes/virtual_*.py` and every `display/*.py` concrete driver filename is a manifest key — keep those files present, each still exporting the same public class (`ReadProbes` for probes, `Display` for displays).
- `uvx ruff format <changed files>` before committing. Commit `git commit -F <file>` (zsh eats backticks). End bodies with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Hindsight:** implementer AND reviewer subagents record their outcome to the `claude_code` bank when done.
- Branch: `refactor/dedup-A-D` off massive-reworks-and-new-ui.

---

### Task 1: Virtual-probe shared reducer base

`probes/virtual_average.py`, `virtual_median.py`, `virtual_highest.py`, `virtual_lowest.py` are identical except the reducer applied to the collected temps: `mean` / `median` / `max` / `min` (verified: virtual_highest vs virtual_lowest differ ONLY by `max`↔`min` + docstrings). Detector: `ReadProbes` class body 33–49 lines duplicated across all four.

**Files:**
- Create: `probes/_virtual_reducer.py` — a base `class ReducerProbe(ProbeInterface)` implementing `read_all_ports(output_data)` exactly as the current modules do, but calling `self._reduce(temp_list)` where they call `mean/max/min/median`. Include the shared `__init__` and `get_device_info` if present in all four.
- Modify: each `probes/virtual_{average,median,highest,lowest}.py` → keep the module + its `class ReadProbes(ReducerProbe)` with a single class attribute `_reduce = staticmethod(<reducer>)` (average→`statistics.mean`, median→`statistics.median`, highest→`max`, lowest→`min`). Keep each module's descriptive docstring.
- Test: `tests/unit/probes/test_virtual_probes.py` must pass UNCHANGED (each `virtual_X.ReadProbes` still exists and behaves identically).

- [ ] **Step 1:** Read all 4 modules + `probes/base.py` `ProbeInterface`. Confirm the ONLY behavioral difference is the reducer (and that `tr` is set to 0 identically, source-lookup elif chain identical).
- [ ] **Step 2:** Create `probes/_virtual_reducer.py::ReducerProbe`, with `read_all_ports` calling `self._reduce(temp_list)`. `_reduce` is a class attribute (unset in the base, or raises if unset).
- [ ] **Step 3:** Rewrite the 4 modules as thin subclasses setting `_reduce`. Preserve docstrings + module `ReadProbes` name.
- [ ] **Step 4:** Run `... uv run pytest tests/unit/probes/test_virtual_probes.py -q` — all pass unchanged. Confirm coverage of the 4 modules + `_virtual_reducer.py` (the base carries the logic now; the 4 stubs are trivially covered).
- [ ] **Step 5:** ruff format + commit (`refactor(probes): extract shared reducer base for virtual aggregator probes`).

---

### Task 2: flexobject InputNumber/InputNumberSimple + `_draw_letterspaced_text`

Detector: `InputNumber._draw_object` (1439–1564) vs `InputNumberSimple._draw_object` (1687–1812) — 126 lines, only 16 differ, EVERY diff a hard-coded position/size constant. Also the class bodies (1616–1679 vs 1818–1881, 64 lines) duplicate. Separately, `_draw_letterspaced_text` is triplicated verbatim in `DutyPill` (2190–2216), `HopperVertical` (2351–2377), `HeaderBar` (2472–2498).

**Files:**
- Modify: `display/flexobject.py`
- Test: `tests/ui/test_flexobject_coverage.py` + the other `tests/ui/test_flex_*` — all must stay green (flexobject is ~97% covered).

- [ ] **Step 1 (InputNumber):** Read both `InputNumber` and `InputNumberSimple` classes fully. Lift the ~16 differing geometry values into class-level constants on each class (e.g. `_ENTRY_POS`, `_ENTRY_SIZE`, `_BTN_...`). Move the shared `_draw_object` body to the common parent (or make `InputNumberSimple` subclass `InputNumber`) so the drawing logic exists once and reads `self.<geometry const>`. Keep BOTH class names/public API intact (they may be referenced by the flex type map). Verify the rendered output is identical by the existing tests' pixel/size assertions.
- [ ] **Step 2 (letterspaced text):** Extract the identical `_draw_letterspaced_text` into a single shared method on the common `FlexObject` base class (or a module-level helper) and have `DutyPill`/`HopperVertical`/`HeaderBar` call it. Confirm no signature/behavior change.
- [ ] **Step 3:** Run `... uv run pytest tests/ui -q` — green, no regressions. Confirm `display/flexobject.py` coverage is unchanged or higher.
- [ ] **Step 4:** ruff format + commit (`refactor(display): dedup flexobject InputNumber variants + letterspaced-text helper`).

**NOTE:** `FlexObject_TypeMap` in `_base_flex.py` resolves widget classes by name dynamically — confirm both `InputNumber` and `InputNumberSimple` remain importable class names after the refactor (do not merge them into one name).

---

### Task 3: Hoist shared flex input/loop handlers into `_base_flex`

Detector: `Display._process_button` is duplicated between `display/_base_dsi.py` (329–383) and `display/ili9341f.py` (377–431) — 55 lines. The `_display_loop` inner block is duplicated too (`_base_dsi` 170–209 vs `ili9341f` 185–224, 40 lines). Both are flex drivers subclassing `_base_flex.DisplayBase`.

**Files:**
- Modify: `display/_base_flex.py` (add the shared handler(s) to `DisplayBase`), `display/_base_dsi.py` and `display/ili9341f.py` (remove the now-inherited copies).
- Also inspect `_process_touch` / `_process_input` — if they are likewise byte-identical across these drivers, hoist them in the same task.
- Test: `tests/ui/test_pygame_qt_drivers.py`, `test_fixed_drivers_methods.py`, `test_dsi_module.py`, and any ili9341f coverage — all green.

- [ ] **Step 1:** Diff `_process_button` (and `_process_touch`/`_process_input`, and the `_display_loop` shared block) between `_base_dsi.py` and `ili9341f.py`. Confirm they are behaviorally identical (identifiers/whitespace only). If they diverge in any meaningful way, hoist ONLY the identical portions and leave per-driver overrides — do not force-merge divergent logic.
- [ ] **Step 2:** Move the identical method(s) up to `_base_flex.DisplayBase`. Delete the copies from `_base_dsi.py` and `ili9341f.py` so both inherit. For the `_display_loop` shared inner block, extract it to a helper method on the base that each driver's loop calls (only if the surrounding loop is otherwise identical; if not, leave `_display_loop` per-driver and extract just the shared inner helper).
- [ ] **Step 3:** Verify: construct both drivers via the existing harness; drive the input/menu paths; assert identical behavior to before. Full `tests/ui` green.
- [ ] **Step 4:** ruff format + commit (`refactor(display): hoist shared flex input/menu handlers into _base_flex`).

**CAUTION:** These are input-dispatch handlers reached by real hardware buttons/touch. Preserve behavior exactly; the display tests are the safety net. Do not start a real `_display_loop`.

---

### Task 4: `_base_fixed` internal dedup (icon draws + `_display_current` branch)

Detector (same-file): `_DisplayBase._display_current` has two near-identical branches (813–848 vs 853–888, 36 lines). The icon-draw methods are pairwise near-identical: `_draw_ignitor_icon` (502–521) vs `_draw_pause_icon` (571–590); `_draw_notify_icon` (525–544) vs `_draw_recipe_icon` (548–567) — 20 lines each, differing by the icon asset/label.

**Files:**
- Modify: `display/_base_fixed.py`
- Test: `tests/ui/test_fixed_*` + `test_fixed_drivers_methods.py` — all green.

- [ ] **Step 1 (icons):** Read the four `_draw_*_icon` methods. Extract a single parameterized helper (e.g. `_draw_status_icon(self, icon_image_or_name, ...)`) capturing the shared drawing; have the four methods call it with their differing asset/position. Keep the four public method names (they are called by name from `_display_current`/status logic).
- [ ] **Step 2 (`_display_current`):** Read the two near-identical branches. Factor the shared body into a helper the two branches call with their differing inputs. Preserve the exact branching condition and outputs.
- [ ] **Step 3:** Verify: `... uv run pytest tests/ui -q` green; the fixed-driver golden/render tests (`test_fixed_base_golden.py`) unchanged. Confirm `_base_fixed.py` coverage unchanged or higher.
- [ ] **Step 4:** ruff format + commit (`refactor(display): dedup _base_fixed icon-draw + _display_current branches`).

---

## Self-Review
- A: 4 probe modules → base + 4 reducer stubs; filenames preserved. B: flexobject InputNumber consolidation + letterspaced-text helper; both class names preserved for the type map. C: flex input/loop handlers → `_base_flex`; only identical portions hoisted. D: `_base_fixed` icon + branch dedup; public method names preserved.
- Every task keeps registry filenames and public class/method names; each verifies against the existing tests (no test weakening).
- Out of scope (deliberate): mpc experiment scripts, test-file duplication, inherent hardware-init glue, and the control-critical `pid_clamping` variants (Task E, deferred).
